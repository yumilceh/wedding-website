{
  "name": "wedding-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "live-server --port=8080",
    "serve": "http-server ./ -p 8080 -c-1",
    "clean": "rimraf dist",
    "build": "npm run clean && copyfiles -u 1 \"**/*\" dist -e node_modules -e dist -e .git -e package.json -e package-lock.json"
  }
}
